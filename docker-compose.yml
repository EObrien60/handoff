services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: handoff
    ports: ["5544:5432"]
    volumes: ["handoff_pg:/var/lib/postgresql/data"]
volumes: { handoff_pg: {} }
