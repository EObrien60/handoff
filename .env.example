# Postgres connection string
DATABASE_URL="postgresql://user:password@localhost:5432/handoff"

# App
APP_URL="http://localhost:3000"
APP_ROOT_DOMAIN="handoff.local"   # {org-slug}.{APP_ROOT_DOMAIN}

# Email (transactional) — set when wiring notifications
# RESEND_API_KEY=""
# EMAIL_FROM="Handoff <hello@handoff.app>"

# Stripe (subscriptions) — set when wiring billing
# STRIPE_SECRET_KEY=""
# STRIPE_WEBHOOK_SECRET=""

# Object storage (S3/R2) — set when wiring file uploads
# STORAGE_BUCKET=""
# STORAGE_ENDPOINT=""
# STORAGE_ACCESS_KEY_ID=""
# STORAGE_SECRET_ACCESS_KEY=""
